deployment:
  tasks:
    - export DEPLOYPATH=/home/seatafri/visionpluswealth.com/vision_plus_wealth

    # Put site in maintenance mode (optional but pro)
    - php $DEPLOYPATH/artisan down || true

    # Copy project files
    - /bin/cp -R app $DEPLOYPATH
    - /bin/cp -R bootstrap $DEPLOYPATH
    - /bin/cp -R config $DEPLOYPATH
    - /bin/cp -R database $DEPLOYPATH
    - /bin/cp -R resources $DEPLOYPATH
    - /bin/cp -R routes $DEPLOYPATH
    - /bin/cp -R vendor $DEPLOYPATH
    # Ensure public directory exists
    - mkdir -p $DEPLOYPATH/public

    # Copy public files
    - /bin/cp -R public/assets $DEPLOYPATH/public
    - /bin/cp -R public/build $DEPLOYPATH/public || true
    - /bin/cp public/index.php $DEPLOYPATH/public
    - /bin/cp public/.htaccess $DEPLOYPATH/public
    - /bin/cp public/favicon.ico $DEPLOYPATH/public || true
    - /bin/cp public/robots.txt $DEPLOYPATH/public || true
    - /bin/cp public/sitemap.xml $DEPLOYPATH/public || true
    - /bin/cp public/brand-guide.html $DEPLOYPATH/public || true
    - /bin/cp -R public/images $DEPLOYPATH/public || true
    - /bin/cp -R public/branches $DEPLOYPATH/public || true

    - /bin/cp artisan $DEPLOYPATH

    # Fix permissions so web server can read all files
    - find $DEPLOYPATH/public/assets -type f -exec chmod 644 {} \;
    - find $DEPLOYPATH/public/assets -type d -exec chmod 755 {} \;


    # Clear Laravel caches
    - php $DEPLOYPATH/artisan config:clear
    - php $DEPLOYPATH/artisan cache:clear
    - php $DEPLOYPATH/artisan view:clear
    - php $DEPLOYPATH/artisan route:clear

    # Run migrations (safe for prod)
    - php $DEPLOYPATH/artisan migrate --force

    # Bring site back up
    - php $DEPLOYPATH/artisan up || true
